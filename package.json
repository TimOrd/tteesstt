{
  "dependencies": {
    "node-sass": "^7.0.0",
    "copyfiles": "^2.2.0"
  },
  "scripts": {
    "sass": "node_modules/.bin/node-sass src/style.scss -o dist/",
    "build": "npm run sass && node build.js"
  }
}
